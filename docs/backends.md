# Backends

```{note}
This page has moved. The backend documentation is now split into a comparison
overview and one page per backend.
```

```{toctree}
:hidden:

backends/console
backends/file
backends/redis
backends/valkey
backends/mqtt
```

Start with {doc}`backends/index` for the comparison table and guidance on which
backend to use.
